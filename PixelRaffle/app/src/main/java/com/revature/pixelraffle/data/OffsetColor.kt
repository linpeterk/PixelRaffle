package com.revature.pixelraffle.data

import androidx.compose.ui.geometry.Offset
import androidx.compose.ui.graphics.Color

data class OffsetColor(

    val offset: Offset = Offset(0f,0f),
    val color: Color = Color.Red,

    )